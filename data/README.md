
# Dataset

This project expects `hero-network.csv` in this directory. The dataset is not committed here because it is third-party material and is comparatively large. Obtain the Marvel hero-network dataset from its original distribution source, then place the file at:

`data/hero-network.csv`

The notebook uses the first two columns as `hero1` and `hero2` and removes null rows and self-loops before analysis.
